<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>{% block head_title %}{% endblock %}</title>
    <meta name="description" content="{% block head_description %}{% endblock %}">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="apple-touch-icon" href="apple-touch-icon.png">
    <link href="{{ siteUrl('vendor/components/normalize.css/normalize.min.css') }}" rel="stylesheet">
    <link href="{{ siteUrl(settings['assets.path'] ~ 'css/main' ~ asset.min ~ '.css') }}" rel="stylesheet">
    {% block head_end %}{% endblock %}
</head>
<body>
    <!--[if lt IE 8]>
    <p class="browserupgrade">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
    <![endif]-->

    <div class="header-container">
        <header class="wrapper clearfix">
            <h1 class="title">{% block title %}{% endblock %}</h1>
            {% include "_partials/nav.twig" %}
        </header>
    </div>

    <div class="main-container">
        <div class="main wrapper clearfix">
            {% if message %}
                <div class="alert {% if errors %}alert-danger{% else %}alert-success{% endif %}">{{ message }}</div>
            {% endif %}
            {% block content %}{% endblock %}
        </div>
    </div>

    <script src="{{ siteUrl('vendor/components/zepto/zepto.min.js') }}"></script>
    {% block body_end %}
    <script src="{{ siteUrl(settings['assets.path'] ~ 'js/main' ~ asset.min ~ '.js') }}"></script>
    {% endblock %}
    {% include "_partials/tracking.twig" %}
</body>
</html>